# Selected after a live connectivity check on 2026-09-14.
# Switch to https://registry.npmjs.org/ when the official registry is faster.
registry=https://registry.npmmirror.com/
fetch-retries=2
fetch-timeout=30000
